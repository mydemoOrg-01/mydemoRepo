---
- name: Print sample text
  hosts: localhost
  tasks:
    - name: Print statement-1
      ansible.builtin.debug:
        msg: I can do it.

    - name: Print statement-2
      ansible.builtin.debug:
        msg: Yes yes I can do this.
